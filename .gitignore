__pycache__/
*.pyc
.venv/
venv/
.env
*.db
*.sqlite3
data/
generated_cvs/

# Real config with your PII / actual criteria — .example.yaml files are the
# tracked templates; these are your filled-in versions and stay local-only.
config/filters.yaml
config/cv_library.yaml

# Google OAuth client secret + stored token — real credentials, never commit.
secrets/
